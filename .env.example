# Root reference — real values live in server/.env (see server/.env.example).
# On Vercel, set these in Project Settings > Environment Variables:
#   DATABASE_URL, SUPABASE_URL, SUPABASE_SERVICE_KEY, JWT_SECRET, STORAGE_DRIVER
#   SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, MAIL_FROM, CLIENT_ORIGIN
#
# STORAGE_DRIVER selects where uploads go: local (./uploads) | supabase | imagekit
#
# ImageKit.io (STORAGE_DRIVER=imagekit) — files land under /bbg-groupbuy/<bucket>:
#   IMAGEKIT_PUBLIC_KEY=public_xxxxx
#   IMAGEKIT_PRIVATE_KEY=private_xxxxx        # server-only secret
#   IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your_id
