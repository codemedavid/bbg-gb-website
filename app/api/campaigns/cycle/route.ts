// Kept for callers that still POST here. "Start new cycle" is ONE control for
// both boards — see app/api/admin/cycles/route.ts, which this re-exports.
export { POST } from '@/app/api/admin/cycles/route';
